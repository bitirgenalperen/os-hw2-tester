- put hw2 executable in this directory
    - default run by "python3 tester.py"
    - run by "python3 tester.py testCase testRepeat"
        - if no other argument given default values for testCases is 25 and testRepeat is 5
    - run by "python3 tester.py -c testCase testRepeat"
        - "-c" flag helps to choose specific case
    - run by "python3 tester.py -l testCase1 testCase2 ... testCaseN testRepeat"
        - "-l" flag helps to choose list of cases to run

        - testCase : indicate which testcases to run -- from 1 to testCase --
            - if "-c" used => testCase indicates which case to run
            - if "-l" used => testCase1 testCase2 ... testCaseN cases will run
        - testRepeat : indicate how many times to run each testcase  